import React from 'react'
import { HomeView } from 'app/routes/Home/components/home.component'
import { render } from 'enzyme'

describe('(View) Home', () => {
  let _component

  beforeEach(() => {
    //_component = render(<HomeView />)
  })

  it('Displays bar chart: Stats By Movie', () => {
    
    //const statsByMovie = _component.find('#dashboard-stats-by-movie');
    //expect(statsByMovie).to.exist;

  })
})
